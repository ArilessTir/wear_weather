import 'package:flutter/material.dart';

class LookPage extends StatelessWidget {
  const LookPage({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(
      title: const Text('Looks'),
    );
  }
}
